# Transport split workspace

This top-level folder separates transport-specific artifacts so COM and TCP work can evolve independently.

- `variants/com`: serial/COM working baseline
- `variants/tcp`: TCP migration scaffold
- `variants/common`: transport-agnostic shared files
